{
  "name": "collab-with-me",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "npm run dev",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "compute:version": "bash ./scripts/compute-version.sh",
    "build:ci": "npm run build && node ./scripts/inject-version.js"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.1"
  }
}
