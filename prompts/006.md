# ATDD next scenario

Let's implement the next scenario: Move card between any states freely

```
scenario("Move card between any states freely", () => {
  given.userIsAuthenticated("alice");
  given.cardExists({ title: "Draft spec", column: "Todo" });
  when.userMovesCard("Draft spec", "Waste");
  then.boardShowsCard("Draft spec", "Waste");
  then.changeIsPersisted();
});
```

## chats response

Expanded the ATDD suite and wired behavior/UI for moving cards. Added board move support and a new scenario (tests/atdd/move-card.spec.ts), extending the DSL (tests/atdd/dsl.ts) with given.cardExists and when.userMovesCard. Board service now supports moves with persistence (src/board.ts). The feedback server/API gained a move endpoint and validation helper (src/server.ts), and the UI now renders move buttons with click handling plus minor styling (public/main.js, public/styles.css). Tests: npm test (2 passing).

Next steps: 1) Add delete/list scenarios and endpoints; 2) swap in durable storage (file/Git-backed) instead of in-memory; 3) layer in auth/SSE once ready.

6 files changed
+142
-13

Undo

View all changes
main.js
+50
-10

styles.css
+16
-0

board.ts
+15
-0

server.ts
+38
-3

dsl.ts
+14
-0

move-card.spec.ts
+9
-0
